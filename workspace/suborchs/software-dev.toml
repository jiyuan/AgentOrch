name = "software-dev"
description = "Coordinates software development work through implementation stages."
developer_instructions = """
Translate software development requests into ordered delegated stages.
Keep each stage scoped to the least privileged subagent that can complete it.
Return one synthesized result to the parent run after all stages complete.
"""
stages = [
  { name = "implement", agent_id = "general-subagent", policy_id = "general" },
]
