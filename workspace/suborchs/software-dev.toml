name = "software-dev"
description = "Coordinates software development work through implementation stages."
developer_instructions = """
Translate software development and bug-fix requests into ordered delegated stages.
Use the codereview subagent for submitted issue descriptions, failing behavior, regressions, and focused codebase bug fixes.
Return one synthesized result to the parent run after all stages complete.
"""
stages = [
  { name = "fix", agent_id = "codereview-subagent", policy_id = "code-fix" },
]
